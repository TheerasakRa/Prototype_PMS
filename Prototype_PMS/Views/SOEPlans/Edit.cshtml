@model Prototype_PMS.Models.SOEPlan

@using (Html.BeginForm("Edit", "SOEPlans", FormMethod.Post, new { role = "form", id = "myform", name = "myform" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>SOEPlan</h4>
        <hr />
        <div class="form-group col d-flex justify-content-end align-items-center">
            @Html.Label("สร้างโดย", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col">
                @Html.DisplayFor(model => model.CreateBy, new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>
        <div class="row mb-3 my-2">
            <label class="col-sm-2 col-form-label">ระยะเวลาการเข้าร่วม</label>
            <div class="col-md-5">
                <div class="row">
                    <div class="col">
                        <p>
                            @Html.DropDownList("StartYear", ViewBag.StartYearList as IEnumerable<SelectListItem>, new { @class = "form-control", @placeholder = "Select Year", @id = "startYearDropdown" })
                        </p>
                    </div>
                    ถึง
                    <div class="col">
                        <p>
                            @Html.DropDownList("EndYear", ViewBag.EndYearList as IEnumerable<SelectListItem>, new { @class = "form-control", @placeholder = "Select Year", @id = "endYearDropdown" })
                        </p>
                    </div>
                </div>
            </div>
        </div>




        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
